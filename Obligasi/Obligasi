#pokok= nilai nomminal
#r=tingkat kupon
#i=tingkat yield
#m=banyak pemberian bunga dalam 1 tahun
#t= tahun

setwd("D:\\PENTING!!!\\kuliah semester 4\\psk r")
obligasi<-function(pokok,r,i,n){
  V=1/(1+i)
  a=((1-(V^n))/i)
  P=(pokok*r*a)+(pokok*(V^n))
  return(P)
}

